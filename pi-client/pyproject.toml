[project]
name = "smarthome-pi-client"
version = "0.1.0"
description = "SwitchBot CO2センサー BLE スキャナー → Lambda API クライアント"
requires-python = ">=3.11"
dependencies = [
    "bleak>=0.22",
    "httpx>=0.27",
]

[tool.uv]
dev-dependencies = []
